/**
 * Variables de entorno de producción (sustituye a `environment.ts` en build prod).
 */
export const environment = {
  production: true,
  /** Si la API vive en otro origen, configurar aquí la URL base (p. ej. `https://api.ejemplo.cl`). */
  apiBase: "",
};
